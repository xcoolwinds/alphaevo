# AlphaEvo — Self-Evolving Stock Strategy Research Agent

# Core (required)
pydantic>=2.0
typer>=0.9
rich>=13.0
pandas>=2.0
pyyaml>=6.0
tenacity>=8.2

# Default data source
yfinance>=0.2.0

# Optional: LLM support (needed for strategy create/evolve)
# litellm>=1.40
